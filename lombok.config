# is telling Lombok that this is the root
# directory and that it shouldn’t search parent directories for more
# configuration files (you can have more than one Lombok config files
# in different directories/packages).
config.stopBubbling = true

# is telling Lombok to add @lombok.Generated annotation to all generated methods.
lombok.addLombokGeneratedAnnotation = true
